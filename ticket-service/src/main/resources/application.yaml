spring:
  application:
    name: ticket-service
server:
  port:
    8890
eureka:
  client:
    fetch-registry: true
    eureka-server-u-r-l-context: http://localhost:8761      

logging:
  level:
    com:
      benali:
        controller: DEBUG
        feign: DEBUG
        client:
          config:
            default:
              logger-level: DEBUG
  